# Test environment variables
DJANGO_SECRET_KEY=test-secret-key-for-testing-only
DEBUG=True

# Celery Configuration for tests
CELERY_WORKER_CONCURRENCY=1 # single worker for tests
CELERY_PARAGRAPH_BATCH_SIZE=2 # smaller batches for faster tests
CELERY_WORKER_POOL=solo # single-threaded for predictable tests
CELERY_ENCRYPTION_KEY=test-encryption-key-32-bytes-long
CELERY_MAX_TASKS_PER_CHILD=10 # frequent recycling for clean test state